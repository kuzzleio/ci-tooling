name: "Danger JS"
on: [pull_request]

jobs:
  check-branch:
    name: Check Pull Request Branch
    uses: kuzzleio/ci-tooling/.github/workflows/branch_check.yaml@branch-check
    secrets: inherit
    with:
      rules: '{"master":["2-dev"]}'
  check-branch2:
    name: Check Pull Request Branch2
    uses: kuzzleio/ci-tooling/.github/workflows/branch_check.yaml@branch-check
    secrets: inherit
    with:
      rules: '{"master":["2-dev"]}'